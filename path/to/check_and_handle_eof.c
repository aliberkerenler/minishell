// Assuming check_and_handle_eof is a C function
#include <stdio.h>
#include <stdbool.h>

void check_and_handle_eof(bool interactive) {
    // Your existing logic...

    if (interactive) {
        printf("exit\n");
    }
}